<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FishOn.Pages_MVs.ProvisioningPages.MyFish.MyFishByWayPoints"
             Title="By Way Point">

  <StackLayout>
    <ScrollView HorizontalOptions="FillAndExpand">
     
    <ListView x:Name="speciesHeaderListView"
              ItemsSource="{Binding FishCaughtByWayPoint, Mode=TwoWay}"
              GroupDisplayBinding="{Binding WayPointName}"
              GroupShortNameBinding="{Binding WayPointNameShortName}"
              IsGroupingEnabled="true"
              HasUnevenRows="true"
              ItemTapped="FishOnSelectedAsync">
      <ListView.GroupHeaderTemplate>
        <DataTemplate>
          <ViewCell Height="40">
            <StackLayout VerticalOptions="FillAndExpand"
                         Padding="5"
                         BackgroundColor="#3498DB">
              <Label Text="{Binding WayPointName}" TextColor="White" VerticalOptions="Center" HeightRequest="40" FontSize="24"/>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.GroupHeaderTemplate>

      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal" Padding="5,5">
              <Label Text="{Binding Species.Name}" FontSize="18"></Label>
              <Label HorizontalOptions="EndAndExpand" Text="{Binding DateTimeCaughtString}"></Label>

            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
      
    </ScrollView>
    <ActivityIndicator IsRunning="{Binding IsBusy}"></ActivityIndicator>
  </StackLayout>
  
</ContentPage>